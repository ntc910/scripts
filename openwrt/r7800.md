# Must Setup
- Config the firewall
- Choose wan in wlan interface config ?conflict?


uci set dropbear.@dropbear[0].GatewayPorts='1'
uci commit dropbear
/etc/init.d/dropbear restart

```bash
scp root@$107.98.150.183:/etc/config/* ./

scp root@$107.98.150.183:/etc/config/firewall ./
```

```fish

```